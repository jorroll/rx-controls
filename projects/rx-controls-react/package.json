{
  "name": "rx-controls-react",
  "version": "0.7.5",
  "main": "main.js",
  "module": "module.js",
  "types": "public-api.d.ts",
  "sideEffects": false,
  "peerDependencies": {
    "rxjs": "^6.0.0",
    "react": "^17.0.2"
  },
  "dependencies": {
    "rx-controls": "^0.7.4",
    "rxjs-hooks": "^0.7.0",
    "tslib": "^2.0.0"
  },
  "devDependencies": {
    "ts-essentials": "^7.0.1",
    "classnames": "^2.3.1"
  },
  "scripts": {
    "test": "jest --config './jest.config.js'",
    "clean": "rm -rf build",
    "types": "tsc",
    "build": "yarn clean && yarn types && ncp package.json build/package.json && rollup -c rollup.config.js"
  }
}
